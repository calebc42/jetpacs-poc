# JA-6 files audit — historical citation map

Status: completed milestone. This path remains because the JA-6 regression
block in `test/jetpacs-files-test.el` cites it. It is not a current audit or
specification.

JA-6 established that file operations use canonical paths and configured-root
containment, reject remote or ineligible paths, sort and bound directory data,
avoid prompts inside dispatch, and preserve confirmation before destructive
actions. Current behavior is owned by `emacs/jetpacs-files.el` and
`test/jetpacs-files-test.el`; launcher behavior is owned by
`emacs/jetpacs-launcher.el` and its tests.

For current architecture and security rules, see
[`ARCHITECTURE-POC3.md`](ARCHITECTURE-POC3.md) and
[`SECURITY.md`](SECURITY.md).

Original audit:

```sh
git show 6251e457aaadba8d2daf9ead3d305a7676412212:docs/AUDIT-ja6-2026-07-28.md
```
