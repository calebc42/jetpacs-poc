# PLAN — Glasspane reimagined: ground-up strangler rebuild onto the ratified PARA IA

**Status: DRAFT, awaiting ratification — produced 2026-08-15.** Nothing below executes before OR-1 is ruled.

Repo: `/home/calebc42/pkb/projects/jetpacs/jetpacs/llm-poc-3` @ **c63935d** (slop-fork/main). Written 2026-08-15, the same day two ratifications landed: **81556eb** (CHROME-VOCABULARY v4 — the full-bar APP-PRIMARY form + the S8 root-only drawer-injection rule) and **c63935d** (`docs/PLAN-glasspane-para.md`, PA-0..PA-5 — the fresh five-tab Agenda-rooted IA, ratified by Caleb). Both are doc-only commits; every code cite below was re-verified at this tree state (code identical to 8965e40).

**Authority:** the ratified PARA IA (PLAN-glasspane-para.md §0) + the surviving machinery half of `docs/PLAN-glasspane-rework.md` (GR-0..GR-7) + a three-candidate reimagining workflow (2026-08-15: three ground-up drafts judged on daily-driver value, architectural leverage, and landability; this doc is the synthesis — the migration-first candidate is the spine, with the winning mechanisms of the other two grafted where they scored).

**Companion docs:** `docs/PLAN-glasspane-rework.md` — §6 is the per-rung supersede/coexist map; that doc is NOT edited here; §9 queues its stamp. `docs/PLAN-glasspane-para.md` — its IA is adopted **verbatim**; this plan contests execution substrate only; §9 queues its stamp. `docs/PLAN-jetpacs-debt-and-scaffold.md` — S8–S11 are the landed chrome floor this plan builds on.

**Ladder prefix is MG- (migration/ground-up)** — fresh per the D-6 label-collision rule (GR-, PA-, G0–G9, R2/R4/R6, S1–S11 taken; a session resuming "MG-5" from this doc alone cannot land on another ledger). Open rulings in this doc are numbered OR-1..OR-9; cite them from other docs as "reimagined OR-N" (rework OR-1..OR-5 and PA-OR-1..PA-OR-4 keep their own names and are referenced with their doc qualifier).

Full ERT gate everywhere below = `test/run-tests.sh` — **the runner is an EXPLICIT list; a suite not named there never runs (the K1a merge lesson). Every rung that adds or moves a suite wires it into the list in the same commit.**

---

## §0 Open rulings — all of them, up front (executing a rung that touches one requires the ruling first)

- **OR-1 — SUPERSEDE vs COEXIST against the ratified plans (BLOCKING; decision Caleb's).** This plan asks to **supersede the execution substrate** of the ratified ladders while **coexisting with (adopting verbatim) everything else**: PARA's IA is the fixed target, GR-0..GR-1 and GR-3..GR-6 run as written inside MG-0/MG-1, and the ceremonies/invariants/tripwires carry whole. What supersession touches: (a) the rework's ratified scope sentence — *"the rework = rebase onto the reader/editor primitives + full seam adoption AND an IA/UX restructure"* (PLAN-glasspane-rework.md:21) — "rebase" becomes "strangler rebuild" for the screen half; (b) PARA's §0.8 sentence — *"GR-0..GR-7 survive unchanged"* — amended per §6's map (GR-2 retired, GR-7a split); (c) PA-2/PA-3's edit-in-place delivery — same destinations, same verified designs and gates, re-hosted on a v2 spine behind per-destination flags. **Recommendation: SUPERSEDE**, for three reasons: (1) the daily driver never regresses — the default flag set is a device-verified configuration at every commit, and PA-3's one big-batch flip becomes five independently reversible, phone-settable flips; (2) the two keystones the debt ledger names — glasspane-detail.el (1,949 LOC) and glasspane-ui.el (765 LOC) — are *replaced* on the foundation seams rather than extended, retiring the documented debt classes (private defer-refresh funnel with ~50 call sites across 12 modules, detail's accidental ownership of shared cards, hand-authored FAB per screen at glasspane-ui.el:123-132, the dead reader front-claim corpse) by construction instead of by refactor; (3) it is **no-regret**: MG-0/MG-1 are identical work under either answer, so if OR-1 is REFUSED this plan dies whole and PA-2/PA-3 proceed as written with nothing wasted. The honest cost (§8): transiently ~2× app code and a longer wall clock than PA-2/PA-3.
- **OR-2 — PARA IA is the fixed target.** Confirm this plan contests execution only: five tabs Agenda/Projects/Areas/Resources/Review, Agenda root, no hub, Journal deleted, Search in the top bar, Archive drawer-only, capture FAB (PLAN-glasspane-para.md §0.1-0.7) are all adopted without re-litigation. Recommendation: confirm.
- **OR-3 — Legacy-refactor waiver.** GR-7a's ~20-caller `glasspane-ui--defer-refresh` conversion and GR-2's adapter registration are NOT executed on code scheduled for demolition; legacy modules are frozen bug-fix-only from MG-2. Labor spent converting corpses is labor wasted. Recommendation: waive.
- **OR-4 — v2 module naming/layout.** `glasspane2-*.el` in `emacs/apps/glasspane/` (same directory, canonical in-tree destination; glasspane-vulpea.el exempt — never renamed, the `:worker-lib` resolution). Recommendation: accept.
- **OR-5 — Formally retire GR-2.** The ratified GR-2 rung (reader front-claim → `jetpacs-reader-register 'org` replace-in-place) had its premise deleted in-tree at **fa1fbf6**: `glasspane-org-reader-register` no longer claims the Files seams — it only removes stale registrations, with a recorded ruling quoted in §7. Executing GR-2 as written would now REVERSE that ruling. Recommendation: adopt the in-tree ruling — whole-file org reading stays org-mode-owned; the dead front-claim corpse (whole-file reader, `--files-body`/`--files-actions` builders, `files.filter`/`files.toggle-refile` consumers, the stale :12-19 header commentary) is deleted at MG-2 citing fa1fbf6 as its soak; the live `glasspane-org-reader-subtree` renderer is re-homed into the v2 spine (its surviving consumer: glasspane-detail.el:907-914). Stamp the rework doc.
- **OR-6 — Detail cutover shape.** Whole-screen atomic flip (constant screen id + the verb family re-registers together) vs section-by-section. Recommendation: atomic — the constant id and single-registrar seam make it one reversible ceremony; partial states are where token/stack bugs live.
- **OR-7 — Coexistence budget rules.** Ratify: (a) the **single-minter rule** — only the ACTIVE implementation of a destination mints token sets, audited against `ebp-org-token-sets-max` 32/owner; (b) the **written token census** — each flip's gate records a per-owner set census as an artifact, and the census must show centralized minting REDUCING or holding the resident count, never growing it (the next flip's ceremony diffs against it); (c) the **two-configuration flag-matrix bound** — ERT runs current-default and all-flipped-through-this-rung, nothing combinatorial. Recommendation: accept.
- **OR-8 — Managed-config promotion trigger (FOUNDATION-GAPS #4's own condition).** `jetpacs-org-capture.el` inheriting Glasspane's capture-template seeding at MG-1b makes it the "second app" wanting the managed-config subtree machinery — exactly the promotion trigger the ratified DEFER named. Promote jetpacs-app-store's subtree machinery to a public seam, or hand the new owner a private copy? Recommendation: promote — the DEFER's own condition is met; a silent private copy is the #26-class duplication this whole program exists to end.
- **OR-9 — Durable offline capture conclusion.** Extend the org-mode-owned capture engine with a queueable conclusion payload (template key + field values, `:when-offline "queue"` + ttl) so capture degrades to *queued* instead of `'rejected` offline. Matters more under PARA than ever: `journal.capture` — today's only offline quick-capture path, queued with ttl 86400 (glasspane-journal.el:75,178-181) — is winding down per PA §0.11. Org-mode-owned extension, raised at MG-1b; D-12 (Companion ACTION_SEND intake) remains the first Companion-side follow-on, unchanged. Recommendation: accept.

**Carried forward unchanged (their venues named, not renumbered):** rework **OR-1** (chronometer → jetpacs-org-clock.el — needed by MG-1c; recommend accept), rework **OR-2**/**PA-OR-4** (ef → `emacs/apps/ef-themes/`, gallery → foundation demo tier — recommend rule at MG-8, cheapest then), rework **OR-3** (demo split follows the capture owner at MG-1b), rework **OR-4** (rich-bodies degrade — still required before any table/babel upstream move at MG-8's tail; not needed for parity cutovers), **PA-OR-1/2/3** (category grammar term, registry merge, Review badge — unchanged; the MG-5 review-due habit card is the concrete input for revisiting PA-OR-3 after the MG-6 soak). Also still open elsewhere: the S9 customize tree-up back-arrow question and the OQ3/D-11 strike-span parking decision — named, not defaulted.

## §1 Thesis

S8–S11 finished the chrome scaffolding: drawer, enumeration, global-action placement, and system back are now declarations over registry data (PLAN-jetpacs-debt-and-scaffold.md §4, all [LANDED]). PARA (c63935d) then fixed the IA: five tabs, Agenda root, no hub. What remains genuinely open is the **execution substrate**: PARA reaches its IA by editing the 12,128-LOC, 21-module legacy in place and grafting new destination modules onto legacy helpers. This plan proposes the ground-up alternative: **the same ratified IA, reached by v2 code that strangles the legacy destination-by-destination, flag-gated, never breaking the daily driver.** The bet has four legs:

1. **Org files are the stable interface.** Both stacks read and write the same vault through ONE save funnel; the schema (SCHEDULED/DEADLINE, LOGBOOK, SRSITEMS, :CREATED:/:MODIFIED:, datetree, id: links, vulpea index) is untouched — any addition would need a versioned ruling, and this plan makes none.
2. **Verb registration is the atomic cutover point.** Verb names are global and durable (rework I-1); screens are reached by dispatch; owner "glasspane", surface ids, and the reminder-id shape are permanent wire identifiers (SPEC 13.5, I-2). So "cut over Search" = the v2 module registers `search.open` and the legacy module doesn't, selected by a flag at enable time. The frozen names become the strangler's seam instead of its enemy: `heading.tap` flips from legacy detail to v2 detail without any caller knowing.
3. **v2 modules are born clean on the matured seams** the legacy predates: `jetpacs-app-defer-refresh` as the only push API a handler can reach — enforced by a standing lint, not review vigilance (§5.1); the S8 composed drawer, with Glasspane authoring none; the GR-7b registry FAB and `:badge` threading; S10/S11; the reader/editor hosts. The debt classes die by replacement.
4. **Every rung is reversible.** Per-destination phone-settable flags, flip ceremonies with scripted reverses tested in ERT both directions, 3–7-day soaks, deletions citing soaks — the GR/PA discipline at finer grain than PA-3's one big batch.

What this plan deliberately does NOT do: contest the PARA IA (OR-2); rebuild capture/reminders/clock app-side (ratified: jetpacs-org-mode owns them — v2 CONSUMES the org-generic engines); touch the CRDT/OT hard wall, the D-19 widget/tile STOP, paging (D-13), or the embeddable-reader refusal (map OQ6); convert legacy code to new seams when that code is scheduled for demolition (OR-3).

## §2 Positioning against PLAN-glasspane-rework.md (nothing silently contradicted)

The rework plan is RATIFIED and largely UNEXECUTED. This plan's relationship to it, item by item (the full rung map is §6):

- **The ownership ruling** (rework §0.1: jetpacs-org-mode owns capture AND agenda reminders; device-verified Glasspane code wins the move) — **OBEYED verbatim**: GR-3/GR-4/GR-5 execute as written inside MG-1, ceremonies §5.1–5.3 untouched. This plan treats the ruling as the precedent for engine ownership generally: v2 never rebuilds an org-generic engine app-side.
- **The scope ruling** (rework:21, "rebase onto the reader/editor primitives") — **CHALLENGED as OR-1**, the one blocking ruling. The machinery half of the scope is preserved; only the screen half's construction changes.
- **GR-2** — **RETIRED via OR-5**: its premise was deleted in-tree at fa1fbf6, with a recorded ruling this plan adopts as design input (§7 quotes it). Neither the front-claim nor the ratified replace-in-place adapter is executed; whole-file org reading stays org-mode-owned.
- **The machinery-first ordering argument** (rework §2) — **OBEYED and sharpened**: MG-0/MG-1 land every live-defect fix and ownership cutover before any dual-stack coexistence, and the one save funnel lands before two stacks can ever write the vault — because two stacks writing one vault through two save policies is the single unacceptable risk of any strangler.
- **I-1..I-8, §3 verb contract, §5 ceremonies, §9 tripwires** — **INHERITED whole** (I-6's three-tier pointer reads through PARA §5.3; §3's `tasks.open` and `glasspane.home` rows read through PARA's §2 amendments).
- **Against PARA** (which this plan must also position honestly, since it postdates the rework): PA §0.1-0.7 adopted verbatim; PA-1's pole mechanics consumed untouched; PA-2's per-destination designs (Areas category layering + the 30.1 workaround, Resources walker/cap/hybrid open, Projects group-by-file fold, Review never-dead states) carry over as CONTENT with only the substrate changed; PA-3 is re-parameterized behind the migration flag as MG-6; PA §0.8's "GR-0..GR-7 survive unchanged" is amended by OR-1/OR-3/OR-5.

## §3 Target architecture + IA

**IA (destination state): PARA §0/§5 byte-for-byte.** Full-bar primary pole via `:chrome 'primary :dock-core nil` (the v4-ratified form; the primary dock branch at jetpacs-apps.el:277-283 gains the suppression per PA-1); five tabs with the Agenda badge; Agenda pinned root whose id EQUALS the id `agenda.open` pushes (tab-tap resets free via same-id truncate-and-replace); the Saved page folding views into Agenda; Archive drawer-only via `:tab nil`; top-bar Search as screen-scope action chrome; the three-tier stack (root / one destination via reset+push peers / one drill) fitting `jetpacs-chrome-max-screens` 3 exactly; route honesty via seed-current + note-route + the bounded back-repush; the S8 composed drawer with Glasspane authoring none; capture FAB from the GR-7b registry. PARA I-9..I-14 inherited whole.

**The day spine (grafted product content, inside PARA's frame).** PARA fixes that Agenda is home; this plan specifies what a lived-in home shows. glasspane2-agenda's **day page** composes, top to bottom: (1) a **clock-status card** when clocked in (status render only — the chronometer stays org-mode's engine per rework OR-1); (2) **Schedule** — the timed extraction for the anchor date, shared cards; (3) **Due & carried over** — overdue/deadline items with the one-tap Today/Pick reschedule buttons (direct `heading.schedule`, the verb surviving per PARA §3); (4) a **review-due habit card** — memoised due count + Start, degrading to the install prompt exactly as the current Review top half; it is the concrete input for revisiting PA-OR-3 after the MG-6 soak. Two spine invariants ride this: **ONE date anchor** — HEAD carries two parallel date-nav grammars (the agenda anchor and journal's own day nav); glasspane2-core owns a single anchor (‹ › shift, Today chip, native picker, reset-to-today on leaving the root), ERT-gated, so the duplication dies with the legacy instead of being ported; and **memoisation keyed on (anchor-date, file stamps) for every root-resident section** — the root re-renders on every push (the bottom-first render lesson), so root cards are memo-reads only, with T-5 re-scoped onto the Agenda root through the MG-6 soak.

**Codebase (the delta from PARA's delivery):** a v2 spine plus per-destination modules under `emacs/apps/glasspane/` (OR-4):

- **glasspane2-core.el** — data layer: agenda extraction, todo walk, query routing (vulpea arm vs ebp-org-query, ONE grammar), tags, the single date anchor, the memoisation contract (`ebp-org-with-cache 'glasspane …` — same cache namespace, so the whole-cache invalidation ruling covers both stacks). Golden-equivalence-tested against glasspane-org outputs on a fixture corpus before any screen consumes it.
- **glasspane2-cards.el** — the shared agenda/result card builders and pure formatters, extracted to a module that OWNS them (retiring glasspane-detail.el's accidental ownership and the `glasspane-ui--*`-named formatters glasspane-agenda defines).
- **glasspane2-nav.el** — the ONE destination table (the #26 invariant: one table feeds tabs, drawer nest, deep links; the table is the only place a flag reads) plus the flag→builder/registrar indirection.
- **Destinations:** glasspane2-agenda.el (day/week/month/custom/Saved pages, the day spine, badge fn, **no reminder hook** — reminders are org-mode-owned by then, so the duplicate-pipeline defect class is unrepresentable in v2), glasspane2-projects.el, glasspane2-areas.el, glasspane2-resources.el (+Archive), glasspane2-review.el, glasspane2-search.el, glasspane2-detail.el (+ planning/props/refile verb family), glasspane2-notes.el, glasspane2-table.el.
- **Survive as-is:** glasspane.el (entry/defapp — edited at flips), glasspane-vulpea.el, glasspane-config.el, glasspane-packages.el (provisioning is IA-neutral).
- **Leave the app:** capture/reminders/clock → `emacs/jetpacs-org-capture/-reminders/-clock.el` (ratified + rework OR-1); ef/gallery per rework OR-2/PA-OR-4; demo split per rework OR-3; glasspane-dates.el → `emacs/jetpacs-dates.el`.
- **Transitional only:** **glasspane-mig.el** — the flag registry (`glasspane-mig-<dest>` ∈ {legacy, v2}, choice-of-consts, phone-settable), single-registrar enforcement (exactly one live registration per verb name, asserted at enable and in ERT), ceremony helpers. Dies at MG-8 via `jetpacs-settings-remove-link`.

**Dependency law (MI-6):** v2 modules require glasspane2-* and foundation only — NEVER a legacy glasspane-* module. Shared code moves into the spine or foundation first. Legacy modules are frozen bug-fix-only from MG-2 (OR-3).

**Migration mechanics — the strangler contract:**
- **MI-1 single-registrar:** for every verb name exactly one live registration; a flag flip is a ceremony (disable → re-enable sweep), never a hot re-register (tripwire MT-2).
- **MI-2 schema stability:** v2 writes nothing legacy can't read and vice versa; the vault is the contract.
- **MI-3 one wire identity:** owner "glasspane", surface ids, reminder-id shape, and the durable verb names (`org.clock.*`, `org.capture.show`/`.share`, `share.text`, `tasks.open`, `glasspane.home`) survive byte-identical; retargets follow I-13 (alias + queue-drain ≥ ttl window before delete; `journal.capture` per PA §0.11 is the template).
- **MI-4 daily-driver floor:** at every commit the DEFAULT flag set is a device-verified configuration; v2 paths are opt-in until their ceremony flips the default; the reverse script is the flag.
- **MI-5 coexistence budgets:** the OR-7 trio — single-minter, written census per flip, two-configuration matrix bound; push cadence and frame size watched (MT-3).
- **Flip ceremony template:** announce → `git tag` the tree+APK pair → one commit flips the default + re-registers → device smoke checklist (tablet PORTRAIT, force-stop first, screenshot before every tap — I-7 discipline) → soak (3 days per destination, 7 for the IA flip) with tripwires armed → reverse = flip the flag back (scripted, ERT-tested in both directions).

## §4 Rung ladder

Each rung ends with a gate; the tree stays green and the tablet daily-driver functional throughout. Rollback doctrine inherited verbatim from rework §2.

| Rung | Name | Depends on | Size | Device arm? | RISKY |
|---|---|---|---|---|---|
| MG-0 | Safety floor + governance (= GR-0 modernized) | OR-1 | S | YES | no |
| MG-1 | Shared substrate on the live app (GR-1, GR-3..GR-6 + seams) | MG-0 | L | YES, mandatory | YES |
| MG-2 | v2 spine + golden suite + corpse deletion (OR-5) | MG-1 | M | no | no |
| MG-3 | First strangle: Search | MG-2 | S | YES + 3-day soak | yes |
| MG-4 | PARA-new destinations, born v2 | MG-2 | M | M-x smoke | no |
| MG-5 | Agenda v2 (day spine + Saved page) | MG-2, MG-1a | L | YES + 3-day soak | YES |
| MG-6 | THE IA FLIP (= PA-3 decomposed, flag-reversible) | MG-3..5, S11 deployed | M | YES — the big batch + 7-day soak | YES |
| MG-7 | Detail v2 (deliberately last) | MG-6 | L | YES + 5-day soak | YES |
| MG-8 | Demolition + tail | MG-7 soak | M | YES (exit checklist) | no |

**MG-0 — Safety floor + governance.** Live defects don't wait for rebuilds. (1) Duplicate-alarm mitigation exactly per GR-0: `defvar jetpacs-org-mode-reminders-enabled` default nil ("nil forever") guarding the hook armed at jetpacs-org-mode.el:550-551; `defvar glasspane-agenda-reminders-enabled` default t guarding glasspane-agenda.el:736-737; the org-mode duplicate set cleared via the I-2 ceremony — loser inert BEFORE clear, confirmation callback observed (T-1), never the session mirror. (2) **wants∩supported fix at the CURRENT address:** the packaged init's `:wants` list (emacs/jetpacs-init.el:448-450) omits `"surfaces.notification"` — add it. GR-0's cite targets (`device/init.el:340-343`, `device/emacs-init.el:362-365`) are stale post-fa1fbf6 packaging; this modernization is recorded, not silent. (3) `git tag glasspane-mig-baseline` on the deployed tree+APK pair. (4) glasspane-mig.el skeleton + the flag settings section + `test/glasspane-mig-test.el` wired into `test/run-tests.sh`'s explicit list in the same commit (K1a).
**Gate:** full ERT + hook-iff-flag arms + the GR-0 device arm (one alarm set per timed item, `dumpsys alarm`; reboot → no org-mode re-arm) + `(jetpacs-granted-p "surfaces.notification")` ⇒ t after reconnect.

**MG-1 — Shared substrate (runs on the LIVE app; rebase-shaped by necessity — durable device state and ratified ownership don't strangle).**
- **1a reminders** → `emacs/jetpacs-org-reminders.el` = GR-3 as written (consolidated extractor, ceremony §5.1, id shape `^org-mode\.rem-` checked) **plus the single-writer guard** (§5.9): `jetpacs-reminders-set` gains a registered-pipeline check — a second effective producer per owner SIGNALS instead of clobbering, turning the hook-singleton ceremony from convention into a tripwire.
- **1b capture** → `emacs/jetpacs-org-capture.el` = GR-4 as written (sheet-chain UX moves, org-protocol + prefix filter absorbed, `org.capture.show`/`.share` + `share.text` byte-identical, whole-cache invalidation ruling kept, T-3 armed). Two rulings ride this rung: **OR-8** (capture-template seeding makes the new owner the managed-config "second app" — promote or private-copy, ruled, never silent) and **OR-9** (the durable offline capture conclusion, recorded as an extension request even if built later). Demo org corpus follows per rework OR-3.
- **1c clock** → `emacs/jetpacs-org-clock.el` = GR-5; **blocked on rework OR-1**, which this plan needs ruled because v2 must not rebuild the chronometer. Ceremony §5.3.
- **1d ONE save funnel, hoisted (GR-6a/6b promoted to the migration's keystone — earlier than the GR ladder placed it, because it must exist BEFORE any dual-stack coexistence).** Before-save policy chain with org-crypt re-encryption FIRST and errors ABORTING the write (the jetpacs-editor.el:136-142 discipline: *"swallowing an Org Crypt failure would permit cleartext persistence"*), mtime/tick freshness gates for splice saves, then after-save policy (whole-cache invalidation per the GR-4/GR-6b ruling, `vulpea-db-update-file` guarded, save-refresh seam). `ebp-org-defer-save` threads the same gate — closing the clock-path bypass: its idle save is a plain `save-buffer` that never runs `jetpacs-files-before-buffer-save-hook`, which fires only inside Files' own save gate (defvar jetpacs-files.el:166, run site :1622), and clock buffers (glasspane-clock.el:185,205) never traverse the editor host's setup. Legacy funnels are rewired to DELEGATE: `glasspane-org--save-and-invalidate` (glasspane-org.el:51-62, bare `save-buffer` — the live cleartext hole) and detail.save's last-write-wins. **Ceremony ordering (grafted):** the ciphertext regression pin lands BEFORE any re-pointing, and each legacy-funnel delegation is ONE commit with its own ERT arm — finer-grained reversibility than a single rung. Pin is forever.
- **1e foundation seams land** (§5.2–5.8) — all pre-funded by GR-7b/PA-1, no Glasspane behavior change yet.
**Gate per sub-rung:** GR-3/4/5/6's own gates + ceremonies unmodified; hardware-first-exposure (GR-1's floor arms) for every ERT-only mechanism before anything rebases onto it; 1d device arm: detail-path edit of a decrypted `:crypt:` entry → ssh the file off, verify ciphertext; external-edit conflict → `'rejected` + snackbar, never clobber.

**MG-2 — v2 spine (invisible rung; zero device delta).** glasspane2-core/cards/nav + single-registrar enforcement + the **golden data-equivalence suite** (v2 extraction vs legacy extraction over a fixture corpus — org files as the tested interface) + two grafted standing arms: the **duplicate-wire-id arm** (build each composed v2 screen, assert zero duplicate ids — the Companion-1201 failure mode; v2 screens compose builders drawn from multiple former modules, the exact re-mint hazard) and the **dispatch-extent push lint** (§5.1's enforcement half: no `jetpacs-shell-push`/`jetpacs-chrome-push-screen` reachable inside any defaction dispatch extent across `emacs/apps/` — covering frozen legacy too; three D2 violations re-entered via the S9 sweep alone, so this is mechanical, not review vigilance). The single-anchor grammar lands in core with its ERT arm. **Delete the fa1fbf6 corpse** (OR-5): `glasspane-org-reader-file`, `--reader-body`, `--files-body`, `--files-actions`, the dead refile-list half, `files.filter`/`files.toggle-refile` consumers, the stale header commentary (glasspane-org-reader.el:12-19 still describes the front-claim) — unreachable since fa1fbf6; that interval is the cited soak. `glasspane-org-reader-subtree` is re-homed into the spine.
**Gate:** golden suite green; lint green over all of apps/; grep gates (zero refs to deleted symbols; no v2→legacy `require`).

**MG-3 — First strangle: Search.** glasspane2-search.el behind `glasspane-mig-search`: the same S2 offline-defvar design (persistence is the feature), builder emitting the vetted grammar, cards from glasspane2-cards, deferred refresh via the foundation funnel. Verbs `search.open`/`org.search.run`/`update-filter`/`clear-filters`/`by-tag` flip registrar at the ceremony. Chosen first: smallest blast radius, self-contained state, proves the ceremony template end-to-end.
**Gate (grafted byte-comparability):** golden **render-output** arm — the v2 screen's emitted tree is golden-diffed against the legacy screen's on the fixture corpus, and the two render side-by-side comparable on device BEFORE the registrar flips (the flip must be provably a no-op to the eye before its soak starts); device smoke (query, builder, by-tag chip round trip, offline build); **written token census** (OR-7b) recorded as the gate artifact; 3-day soak; reverse tested.

**MG-4 — PARA-new destinations, born v2 (no legacy counterpart = no strangle needed).** glasspane2-areas / -resources(+Archive) / -projects / -review per PA-2a-e's verified designs — category layering + the 30.1 workaround, per-file condition-case skip, walker cap, hybrid org-drill/Files-handoff, group-by-file fold, never-dead Review states, Habits link — built on the spine, substrate changed, content and gates intact. Hidden until MG-6 (no destination-table rows; verbs registered + M-x reachable for smoke). Review strangles glasspane-srs's SCREEN half only; the org-srs engine discipline (save-inside-engine-form) moves intact.
**Gate:** PA-2's own arms re-hosted + duplicate-id arms + M-x device smoke of each screen.

**MG-5 — Agenda v2.** glasspane2-agenda.el: day/week/month pages (curated month_grid + composed fallback), custom-agenda pages (cap 8), the **Saved page** (PA §5.5 — both registries presented, PA-OR-2 untouched), the destination badge fn, and the **day spine** of §3 (clock card, schedule, due/carried-over with one-tap reschedule, review-due habit card), every section a memo-read keyed on (anchor-date, file stamps). **No reminder hook** — reminders are org-mode-owned since MG-1a. `tasks.filter` moves per PA-2d; `tasks.open` aliases to `projects.open` (I-13). Views' per-view screens re-host on the spine (list/board/calendar renderings); `views.hub` aliases to the Saved page.
**Gate:** ERT + byte-comparability arms for the pages that have legacy counterparts (day/week/month) + duplicate-id arm for the composed Saved page (it merges two registries — the exact composition hazard) + single-anchor arm + device smoke (all pages, badge, saved views' three renderings, swipe/todo-cycle/archive) + written census (the day spine's new sets must hold the count: schedule/carried/journal-successor sets replace, not add) + 3-day soak on `glasspane-mig-agenda`.

**MG-6 — THE IA FLIP (= PA-3a-d decomposed, flag-reversible).** Behind `glasspane-mig-ia` ∈ {legacy-hub, para}: the defapp gains `:chrome 'primary :dock-core nil :fab` (registry capture descriptor — the hand-authored per-screen FAB node, glasspane-ui.el:123-132, retires); glasspane2-nav's table becomes PA-3a's six-row table (five `:tab t` + Archive `:tab nil`); root swap to "glasspane-agenda" with root-id-equals-push-id reset semantics; boot seed + note-route + the bounded back-repush; Glasspane authors NO drawer (S8 composes: nests, relocated Eval/Files, host tail); top-bar Search as a screen-builder `:actions` helper (NOT the S10 global seam — its placement defcustom could re-author it into a FAB and collide with capture); the journal wind-down begins (row out, `journal.capture` → headless datetree-append deprecation alias, landing defcustom dies); `glasspane.home` resets to the Agenda root (name/shape unchanged — I-1). Legacy IA remains one flag-flip away for the whole soak.
**Gate:** the full PA-3 device batch verbatim (boot-state, both form factors, the I-14 back arms, drawer composition, route honesty, slot/token audit with live sets re-counted under single-minter) **plus the grafted airplane-mode arm**: capture and quick-add offline → queue drains on reconnect → replayed payloads file correctly — the only end-to-end exercise of the `journal.capture` deprecation-alias wind-down (I-13). **7-day daily-driver soak**; T-4/T-5 explicitly re-scoped onto the Agenda root (it concentrates both budgets by design and carries both watches through its soak).

**MG-7 — Detail v2 (highest risk, deliberately last; every prior rung ships against legacy detail via stable `heading.tap`).** glasspane2-detail.el: constant-id screen, read mode on the re-homed subtree renderer, edit mode on the jetpacs-editor host, **detail.save through the MG-1d funnel** (last-write-wins dies here for good), the planning/props/tags/refile/todo verb family flipping registrar atomically (OR-6). glasspane2-notes hooks (backlinks/mentions/SRS chip) re-home onto v2 detail's extension seams; glasspane2-table re-homes the table/babel exposure. The one surviving legacy front-claim — detail's `jetpacs-files-editor-actions-functions` depth-90 claim plus the global `ebp-org-file-save-function` rebind (glasspane-detail.el:1930-1934) — is replaced by proper host registration (the actions claim is sanctioned APPEND-seam use and may survive re-recorded; the save-fn rebind dies — the funnel makes it moot), ruled at the rung.
**Gate:** ERT + the heaviest device smoke (all ~20 verbs, crypt round trip, freshness-conflict arm, offline queue + dedupe, duplicate-id arm) + 5-day soak.

**MG-8 — Demolition + tail.** Delete legacy modules, each commit citing its soak: glasspane-search, -views, -journal (after the queue-drain check; the alias survives ≥ one release per PA §0.11), -agenda, -detail, -org-reader, -org's walk/query halves, the ui keystone's hub corpse and private funnel, glasspane-mig.el and all now-constant flags + the flag settings section (`jetpacs-settings-remove-link`). Grep gates: zero refs to deleted symbols; run-tests.sh explicit list re-verified. ef/gallery re-home per the OR ruling; demo split per rework OR-3. **Consolidated exit device checklist** (one day): single-alarm, reboot-rearm, capture e2e **including the airplane-mode queued-replay arm**, chronometer (if GR-5 ran), PARA IA spot-checks, crypt pull-and-verify, desync re-run, back contract (I-14) ⇒ `git tag glasspane-mig-closed`; cross-reference stamps applied (§9); PA-5/GR-10 upstream items proceed decision-gated as written (rework OR-4 still required before the table/babel move).

## §5 New foundation seams (each: contract sketch, size, what it absorbs)

1. **`jetpacs-app-defer-refresh` + the dispatch-extent push lint.** Contract: the mode-neutral deferred-repush funnel (flow-continue + `jetpacs-shell-push` to the flow's surface) — the ONLY push API reachable from a v2 handler — plus a standing ERT suite asserting no `jetpacs-shell-push`/`jetpacs-chrome-push-screen` call is reachable inside any defaction dispatch extent under `emacs/apps/`. Size: S (funnel) + S (lint). Absorbs: GR-7a's foundation half / FOUNDATION-GAPS #17; mechanizes the D2 lesson (three violations re-entered via the S9 sweep alone). The legacy ~50-caller conversion is waived (OR-3) — legacy keeps its private funnel until each caller's module dies.
2. **Shared save-funnel policy hooks (GR-6a/6b hoisted).** Contract: before-save chain — org-crypt re-encryption FIRST, errors abort the write (jetpacs-editor.el:136-142 discipline); mtime/tick freshness gates — then after-save policy — whole-cache `(ebp-org-cache-invalidate)`, guarded `vulpea-db-update-file`, save-refresh seam; `ebp-org-defer-save` threads the same gate. Size: M. Absorbs: GR-6a+6b whole; FOUNDATION-GAPS #8 (save-refresh seam), #9 (shared save policy), #10-adjacent freshness core; closes the I-4 cleartext hole (glasspane-org.el:51-62), detail.save last-write-wins, and the clock-path bypass (jetpacs-files.el:166/:1622 vs glasspane-clock.el:185,205) in one seam.
3. **`:badge` threading through `jetpacs-apps--destination-tabs`.** Contract: destination-plist `:badge` (nullary memoised fn or string, nil-when-zero), validated in `jetpacs-apps--check-destination-list` (jetpacs-apps.el:63), threaded into tab generation. Size: S. Absorbs: GR-7b step 1 — without it the Agenda badge dies at the pole flip (today it rides the hand dock item, glasspane.el:123).
4. **`jetpacs-defapp` `:fab` registry.** Contract: `:fab` descriptor or (SURFACE)→descriptor fn; `jetpacs-chrome--build` injects when the screen authors none, keyed on the SCREEN's owner — S4 guest screens NEVER get the host's FAB. Size: S. Absorbs: GR-7b step 2 / FOUNDATION-GAPS #2; retires the per-screen hand-authored capture FAB (glasspane-ui.el:123-132).
5. **`jetpacs-apps-seed-current` (+ROUTE) + `jetpacs-apps-note-route`.** Contract: boot seed of current app+route at READY; route noting for non-`app.open` navigation + clear-on-reset; the bounded back-repush per PA §5.4. Size: S. Absorbs: the route-honesty gaps GR-8a named and PA-1.5 specifies — REQUIRED here: a root that is also Tab 1 renders unselected at boot without them.
6. **PA-1 full-bar pole mechanics.** Contract: `:dock-core` (default t) suppressing core dock items under primary; `:tab` key filtering rows from tabs before the `seq-take`; the relocation rule composing suppressed core into the drawer. Size: S. Absorbs: nothing pre-existing — ratified in CHROME-VOCABULARY v4 (81556eb), specified in PA-1, unbuilt at HEAD; consumed by MG-6 exactly as specified.
7. **`emacs/jetpacs-dates.el`.** Contract: mechanical promotion of glasspane-dates.el (pure util), prefix grep, suite renamed + rewired. Size: S. Absorbs: FOUNDATION-GAPS #10 / GR-7b step 4.
8. **`jetpacs-settings-remove-link`.** Contract: foundation remover for settings links; used by legacy cleanups and MG-8's flag-section teardown. Size: S. Absorbs: GR-7b step 3.
9. **`jetpacs-reminders` single-writer guard.** Contract: `jetpacs-reminders-set` records the effective producer per owner; a second registration signals instead of clobbering. Size: S. Absorbs: nothing — mechanizes the hook-singleton invariant the GR-3 gate tests by enumeration, hardening T-1's defect class during and after the cutover.
10. **Durable offline capture conclusion on `jetpacs-org-capture`** (OR-9). Contract: a queueable conclusion payload (template key + field values, `:when-offline "queue"`, ttl) replayed through the same engine path on reconnect. Size: S-M, org-mode-owned. Absorbs: the product hole PARA's FAB dialog inherits from `journal.capture`'s wind-down — capture currently requires a live client and answers `'rejected` offline.

App-local, not foundation (recorded boundaries): **glasspane-mig.el**'s single-registrar assertion (dies at MG-8); **glasspane2-cards.el** stays app-side — foundation promotion explicitly deferred until a second consumer (the FOUNDATION-GAPS #4 discipline, whose one legitimate trigger this plan already cashes as OR-8).

## §6 Supersede/coexist map — every GR rung (and every PA rung)

| Rung | Fate under this plan | Where / why |
|---|---|---|
| GR-0 | **SURVIVES** → MG-0 | file targets modernized: the packaged `emacs/jetpacs-init.el:448-450`, not the deleted device inits — recorded, not silent |
| GR-1 | **SURVIVES** → folded into MG-0/MG-1 gates | hardware-before-rebase kept as a per-mechanism rule |
| GR-2 | **RETIRED** (OR-5) | premise deleted in-tree at fa1fbf6; neither front-claim nor adapter replace-in-place executed; corpse deleted at MG-2; subtree renderer re-homed; PA-2b/2c's "after GR-2" soft dependency re-points at the spine's exported subtree builder |
| GR-3 | **SURVIVES** → MG-1a | + the single-writer guard (§5.9); ceremony §5.1 verbatim |
| GR-4 | **SURVIVES** → MG-1b | + OR-8 and OR-9 raised at the rung; ceremony §5.2 verbatim |
| GR-5 | **SURVIVES** → MG-1c | still gated on rework OR-1; ceremony §5.3 verbatim |
| GR-6a/6b | **SURVIVE, PROMOTED EARLIER** → MG-1d | one write path must exist before any dual-stack coexistence; crypt pin lands first; per-funnel delegation commits |
| GR-7a | **SPLITS** (OR-3) | the foundation funnel + lint survive (§5.1; v2 is born on them); the ~50-call-site legacy conversion is WAIVED — legacy keeps its private funnel until each caller's module dies at MG-8 |
| GR-7b | **SURVIVES** → MG-1e | :badge, :fab registry, remove-link, jetpacs-dates — also PA-3a hard deps |
| GR-8a-d | **already SUPERSEDED by PARA** (PA §2) | this plan inherits that supersession; MG-6 delivers PA-3's content |
| GR-9 | **SUPERSEDED** → MG-8 | the demolition list grows to whole legacy modules, not just corpses; deletions-cite-soaks doctrine kept |
| GR-10 | **SURVIVES RESCOPED** → MG-8 tail | = PA-5; each item decision-gated as written (rework OR-4 still required for table/babel) |

PA rungs: **PA-0** landed (c63935d) — respected. **PA-1** survives untouched → consumed by MG-6 (§5.6). **PA-2a-e** content survives, substrate changes → MG-4/MG-5 (same designs, same gates, glasspane2-* on the spine instead of legacy helpers). **PA-3a-d** survive re-parameterized → MG-6 (same table, root swap, drawer handover, device batch; delivered behind `glasspane-mig-ia` with per-destination v2 builders instead of edits to glasspane-ui.el). **PA-4** survives extended → MG-8. **PA-5** survives → MG-8 tail. **If OR-1 is refused, this plan dies whole and PA-2/PA-3 proceed as written** — MG-0/MG-1 are identical work either way (no regret).

## §7 Constraints honored (the ratified sentences, quoted, with compliance or the OR challenging them)

- *"jetpacs-org-mode owns BOTH capture and agenda reminders. … The device-verified Glasspane implementations are the ones with hardware coverage — their code/UX moves under the new owner"* (PLAN-glasspane-rework.md:20). **COMPLIED**: MG-1a/1b execute GR-3/GR-4 as written; v2 consumes the engines and never rebuilds them (MI-6).
- *"the rework = rebase onto the reader/editor primitives + full seam adoption AND an IA/UX restructure"* (PLAN-glasspane-rework.md:21). **CHALLENGED — OR-1** (the blocking ruling): "rebase" → strangler rebuild for the screen half; full seam adoption is kept and strengthened (v2 is *born* on the seams).
- *"PARA replaces GR-8's IA. GR-0..GR-7 survive unchanged and remain prerequisites where named"* (PLAN-glasspane-para.md §0.8). **PARTIALLY CHALLENGED — OR-1/OR-3/OR-5**: GR-0..GR-1, GR-3..GR-6 survive inside MG-0/MG-1; GR-2 is retired on the tree's own ruling; GR-7a splits. Nothing else in PA §0 is touched.
- *"The bar is exactly five destinations: Agenda, Projects, Areas, Resources, Review"*; *"Agenda is home — no hub"*; *"Journal is deleted. Search moves to the top app bar"* (PLAN-glasspane-para.md §0.1/0.2/0.7). **COMPLIED verbatim** (OR-2 confirms the frame): MG-6 ships exactly this table; the day-spine graft changes what the Agenda root *shows*, not what the destinations *are*.
- *"`journal.capture` winds down through a deprecation alias + queue-drain ceremony … never a bare delete"* (PLAN-glasspane-para.md §0.11). **COMPLIED and strengthened**: the alias lands at MG-6, the airplane-mode arm exercises the replay end-to-end at MG-6 and MG-8, deletion waits ≥ one release.
- *"Glasspane does not claim the global Files body/actions seams. … installing the app must not replace vanilla Org Mode merely because a file is inside `org-directory'"* (in-tree ruling, glasspane-org-reader.el:784-789, landed at fa1fbf6; echoed at jetpacs-init.el:168-170). **ADOPTED as design input — OR-5** formalizes it against the ratified-but-stale GR-2, because plan and tree currently disagree and one of them must be stamped.
- **I-1 durable verb names** (rework §1): **COMPLIED** — MI-3; every retarget follows I-13 with alias + drain window; the strangler's whole mechanism depends on the names being frozen.
- **I-2 reminder cutover ordering** (rework §1): **COMPLIED** — §5.1 ceremony verbatim at MG-1a, loser-inert-before-clear, callback observed; the §5.9 guard adds mechanical enforcement.
- **I-4 org-crypt** (rework §1): **COMPLIED, promoted** — MG-1d lands the pin before anything else re-points; ciphertext regression pinned forever.
- **I-6 slot budget / max-screens 3**: **COMPLIED** — PARA §5.3's three tiers adopted; the collection of audits re-runs at MG-6 (PA-3c analog).
- **Standing non-moves and walls:** CRDT/OT hard wall, D-19 widget/tile STOP, the OQ6 embeddable-reader refusal, Saved Searches app-side, srs engine app-side, `glasspane_mobile` never renamed, D-13 paging ledger — **ALL COMPLIED**; none reopened. The one non-move this plan re-touches is FOUNDATION-GAPS #4's managed-config DEFER — reopened **only** through its own ratified trigger condition, as OR-8, never silently.

## §8 Tripwires and costs

Rework §9 T-1..T-6 carried whole (T-5 re-pointed per PARA §8 onto the back-repush + the Agenda-root cards, and re-scoped through the MG-6 soak; PARA's T-7 Areas-walk watch carried). Migration-specific: **MT-1** token-set count under owner "glasspane" approaches 32 during coexistence → audit single-minter compliance, disable inactive minters (the written census is the diff base). **MT-2** duplicate live verb registration (suite + enable-time assert) → abort the flip, flag back. **MT-3** push-cadence/frame-size growth from dual-loaded modules → graduate the D-5 tripwire, profile before the next flip. **MT-4** receipt-rejection spike after any retarget → drain-window violation; restore the alias immediately. **MT-5** any soak-period defect on a flipped destination → flag back first, diagnose second.

**Costs, honestly:** peak tree carries ~2× app code (≈24k LOC transiently); wall clock is longer than PA-2/PA-3 by roughly the spine + golden-suite rungs; every flip is a ceremony; OR-1's ratification load lands before rung one. Bought with it: the daily driver never regresses (MI-4 — PA-3's one-soak-day big batch becomes five independently reversible flips); the 1,949-LOC detail and 765-LOC ui keystones are replaced, not extended; the reminder/capture/save-funnel safety work is front-loaded and identical under either execution, so nothing here delays the live-defect fixes; and rollback of ANY screen-level decision is a phone-settable flag, not a revert.

## §9 Cross-reference stamps (apply on each doc's next edit; this plan does not modify them)

**For `docs/PLAN-glasspane-rework.md`:** > **Reimagined execution (2026-08-15, DRAFT): docs/PLAN-glasspane-reimagined.md (ladder MG-0..MG-8)** proposes strangler-rebuild delivery of the PARA IA in place of edit-in-place; its OR-1 asks to re-scope this plan's §0.2 "rebase" sentence; its OR-5 retires GR-2 on the fa1fbf6 in-tree ruling; GR-7a's legacy caller conversion is waived by its OR-3. GR-0/1/3/4/5/6 execute unchanged inside MG-0/MG-1. Inert until OR-1 is ruled.

**For `docs/PLAN-glasspane-para.md`:** > **Reimagined execution (2026-08-15, DRAFT): docs/PLAN-glasspane-reimagined.md** adopts this plan's IA verbatim and proposes delivering PA-2/PA-3 as v2 modules behind per-destination flags (its §6 map); §0.8's "GR-0..GR-7 survive unchanged" is amended by its OR-1/OR-3/OR-5 if ratified. If its OR-1 is refused, PA-2/PA-3 proceed as written and nothing here applies.

**For `docs/PLAN-jetpacs-debt-and-scaffold.md`:** > **Reimagined execution (2026-08-15, DRAFT):** docs/PLAN-glasspane-reimagined.md, if ratified, funds GR-7b's seams at MG-1e, lands the dispatch-extent push lint (D2 mechanical enforcement) at MG-2, and re-hosts the PA-3 delivery as MG-6. D-12 remains the first Companion-side follow-on after MG-1b.
