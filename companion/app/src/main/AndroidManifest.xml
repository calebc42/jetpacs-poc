<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Loopback sockets still require the INTERNET permission. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SPEC 18.5/18.6: present notification surfaces and reminders. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- SPEC 18.6: reminders fire at an exact wall-clock time. -->
    <!-- Android 14 may deny this special access. Scheduling always has an
         inexact setAndAllowWhileIdle fallback and never assumes exactness. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- SPEC 20.3: the `vibrate` capability. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- Camera permission backs the optional flashlight handler; neither a
         camera nor a flash is required to install Jetpacs. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <!-- The reconnection notification opens Android Emacs directly. Android
         11+ hides installed packages unless they are declared here. -->
    <queries>
        <package android:name="org.gnu.emacs" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- SPEC 15/18.6/21.1: the Companion's durable state is an outbox and a
         cache, never a backup source. Restoring it onto another device would
         replay events this device already delivered, breaking the at-most-once
         guarantee. allowBackup=false kills cloud backup; on Android 12+ it
         does NOT stop device-to-device transfer, so the rules file below
         excludes that channel too. -->
    <application
        android:name=".JetpacsApplication"
        android:label="Jetpacs Companion"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The external widget host must launch this one component. It
             accepts only ACTION_APPWIDGET_CONFIGURE and revalidates the
             supplied ID against this provider before reading or writing. -->
        <activity
            android:name=".WidgetConfigurationActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WidgetActionConfirmationActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.DeviceDefault.Dialog.NoActionBar" />
        <activity
            android:name=".ReminderInteractionActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <service
            android:name=".JetpacsBridgeService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-enabled local loopback bridge for an on-device Emacs process" />
        </service>

        <!-- SPEC 18.6: an armed reminder fires here and posts its notification. -->
        <receiver android:name=".ReminderAlarmReceiver" android:exported="false" />
        <!-- SPEC 18.6: a reminder tap routes into the Section 14 pipeline. -->
        <receiver android:name=".ReminderTapReceiver" android:exported="false" />
        <!-- SPEC 18.5: a notification action tap routes into the Section 14 pipeline. -->
        <receiver android:name=".NotificationActionReceiver" android:exported="false" />
        <receiver android:name=".WidgetActionReceiver" android:exported="false" />
        <receiver android:name=".WidgetStaleReceiver" android:exported="false" />

        <receiver
            android:name=".JetpacsWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/jetpacs_widget_info" />
        </receiver>

        <!-- SPEC 21.5: the system-only SMS broadcast. The sender must hold the
             signature BROADCAST_SMS permission in addition to matching this
             exported component. Message content is never logged. -->
        <receiver
            android:name=".SmsTriggerReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- SPEC 21.5: an armed time.* trigger fires here (our PendingIntent only). -->
        <receiver android:name=".TimeAlarmReceiver" android:exported="false" />
        <!-- SPEC 21.5: re-arm reminders/time triggers and fire boot/timezone
             triggers across reboot and wall-clock changes. Protected system
             broadcasts only, so exported is safe. -->
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <!-- SPEC 21.1: an app update cancels all alarms and stops the
                     app; re-arm reminders + time triggers on the next update. -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Fixed EBP Quick Settings host slots. Export is required for System
             UI; BIND_QUICK_SETTINGS_TILE is a signature permission, so no
             ordinary application can bind or invoke these services. -->
        <service
            android:name=".EbpTile1"
            android:exported="true"
            android:icon="@drawable/ic_qs_jetpacs"
            android:label="Jetpacs 1"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="true" />
        </service>
        <service
            android:name=".EbpTile2"
            android:exported="true"
            android:icon="@drawable/ic_qs_jetpacs"
            android:label="Jetpacs 2"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="true" />
        </service>
        <service
            android:name=".EbpTile3"
            android:exported="true"
            android:icon="@drawable/ic_qs_jetpacs"
            android:label="Jetpacs 3"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="true" />
        </service>
        <service
            android:name=".EbpTile4"
            android:exported="true"
            android:icon="@drawable/ic_qs_jetpacs"
            android:label="Jetpacs 4"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="true" />
        </service>
        <service
            android:name=".EbpTile5"
            android:exported="true"
            android:icon="@drawable/ic_qs_jetpacs"
            android:label="Jetpacs 5"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="true" />
        </service>
    </application>
</manifest>
