# Jetpacs Automations implementation guide

Keep the model inert, the interpreter closed over explicitly admitted forms,
and durable trigger handling separate from app presentation. Depend only on
public EBP and Jetpacs seams. Run the three focused ERT suites through
`test/run-tests.sh` before handoff.
